apiVersion: v1
kind: Secret
metadata:
    labels:
        app.kubernetes.io/name: {{ .Release.Name }}
    name: {{ .Release.Name }}
    namespace: {{ .Release.Namespace }}
type: Opaque
data:
    # Exchanges
    binance.api_key: {{ (.Values.credentials).exchanges.binance.api_key | b64enc }}
    binance.secret_key: {{ (.Values.credentials).exchanges.binance.secret_key | b64enc }} 