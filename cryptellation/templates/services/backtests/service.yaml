apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-backtests
  name: {{ .Release.Name }}-backtests
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-backtests
  template:
    metadata:
      annotations:
        {{if (.Values.debug).forceDeployment }}timestamp: {{ now | quote }}{{end}}
      labels:
        app: {{ .Release.Name }}-backtests
    spec:
      initContainers:
      - name: sql-migrations
        image: {{ ((.Values.services).backtests).image.repository | default "ghcr.io/cryptellation/backtests" }}:{{ ((.Values.services).backtests).image.tag | default (printf "v%s" .Chart.AppVersion) }}
        imagePullPolicy: {{ ((.Values.services).backtests).image.pullPolicy | default "Always" }}
        command: ["worker", "database", "migrate"]
        env:
        # SQL
        - name: "SQL_DSN"
          valueFrom:
            configMapKeyRef:
              key: sql.dsn
              name: cryptellation-backtests
      containers:
      - name: backtests
        image: {{ ((.Values.services).backtests).image.repository | default "ghcr.io/cryptellation/backtests" }}:{{ ((.Values.services).backtests).image.tag | default (printf "v%s" .Chart.AppVersion) }}
        imagePullPolicy: {{ ((.Values.services).backtests).image.pullPolicy | default "Always" }}
        livenessProbe:
          httpGet:
            path: /liveness
            port: {{ (((.Values.services).backtests).health).port | default 9000 }}
        readinessProbe:
          httpGet:
            path: /readiness
            port: {{ (((.Values.services).backtests).health).port | default 9000 }}
        env:
        # Health
        - name: "HEALTH_ADDRESS"
          valueFrom:
            configMapKeyRef:
              key:  health.address
              name: cryptellation-backtests
        # SQL
        - name: "SQL_DSN"
          valueFrom:
            configMapKeyRef:
              key: sql.dsn
              name: cryptellation-backtests
        # Opentelemetry
        - name: "OPENTELEMETRY_GRPC_ENDPOINT"
          valueFrom:
            configMapKeyRef:
              key: opentelemetry.grpc.endpoint
              name: cryptellation-credentials
        # Temporal
        - name: "TEMPORAL_ADDRESS"
          valueFrom:
            configMapKeyRef:
              key: temporal.address
              name: cryptellation-credentials
        resources: {}
status: {}
