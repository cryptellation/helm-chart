{{- if .Values.services.sma.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-sma
  name: {{ .Release.Name }}-sma
  namespace: {{ .Release.Namespace }}
data:
  health.address: {{ (((.Values.services).sma).health).address | default ":9000" }}
{{- end }}